# Notion URL to archive
TARGET_URL=https://yumemi.notion.site/

# Output directory for archived files
OUTPUT_DIR=output

# Browser viewport settings
VIEWPORT_WIDTH=1920
VIEWPORT_HEIGHT=1080

# Page load timeout in milliseconds
PAGE_LOAD_TIMEOUT=60000

# Browser headless mode (true for CI environments, false for development)
HEADLESS=false
